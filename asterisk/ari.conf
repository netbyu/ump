; =============================================================================
; Asterisk REST Interface (ARI) Configuration
; Used by Node.js to control Asterisk in real-time
; =============================================================================

[general]
enabled = yes
pretty = yes
;allowed_origins = http://localhost:3000

[asterisk-mgmt]
type = user
read_only = no
password = your_ari_password_here
