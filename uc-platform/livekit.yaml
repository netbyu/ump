# LiveKit Server Configuration
# For development use only - use proper secrets in production

port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 60000
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false

keys:
  # Development API key - CHANGE IN PRODUCTION
  devkey: secret

logging:
  level: info
  json: false

room:
  auto_create: true
  empty_timeout: 300
  max_participants: 100

turn:
  enabled: true
  domain: localhost
  tls_port: 5349
  udp_port: 3478
