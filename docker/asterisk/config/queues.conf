;
; Queue Configuration
; Queues are loaded from database via Realtime
; This file provides defaults only
;

[general]
persistentmembers = yes
autofill = yes
monitor-type = MixMonitor
shared_lastcall = yes

; Queue templates and static configs go here
; Dynamic queues are managed via the database
